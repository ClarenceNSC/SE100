const domContainer = document.getElementById('root');
const root = ReactDOM.createRoot(domContainer);
root.render (<App/>)

const age = 20;
const beverage = // Write your code here
console.log(beverage); // 'Beer' if age is 18 or more, 'Juice' otherwise

